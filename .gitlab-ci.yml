stages: 
  - build
  - test

build website:
  stage: build
  image: node:20
  script:
    - npm install
    - npm install -g gatsby-cli
    - gatsby build
  artifacts:
    paths:
      - public/

Test artifact:
  stage: test
  script:
    - grep -i "gatsby" ./public/index.html
    - grep "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" ./public/index.html
